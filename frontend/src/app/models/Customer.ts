import { Title } from '@angular/platform-browser';

export interface Customer {
  avatar?: string,
  id?: number,
  name?: string,
  last_name?: string,
  email?: string,
  credit?: number,
  password?: string

};
