import { Title } from '@angular/platform-browser';

export interface Game {
  image?: string,
  id?: number,
  title?: string,
  console?: string,
  created_at?: Date,
  genre?: string,
  price?: number,
  old_price?: number,
  usk?: number,
  stock?: number,
  maker?: string,
  location?: string

};
